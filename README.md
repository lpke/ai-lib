# AI Prompt Library

Shared prompt and skill assets for agentic workflows.

## Conventions

- `prompts/` contains provider-neutral prompt text
- `skills/` contains agent-specific packaging around the same behavior
- When using prompts/skills in a project (or globally), they can be copied or
symlinked from here

