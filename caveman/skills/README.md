# Skills

Agent-specific packages for `caveman`.

## Packages

- `claude/caveman/`: Claude skill package for `~/.claude/skills/caveman` or `.claude/skills/caveman`.
- `codex/caveman/`: Codex skill package for `.agents/skills/caveman`, with optional hook support.

Each package owns its own install notes because agents differ in discovery paths and config.

